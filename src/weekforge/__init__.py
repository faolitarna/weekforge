"""Weekforge — CLI for the training week planning and summarization lifecycle.

Entry point: weekforge.cli.app (registered as the `weekforge` console script).
Public surface for callers: the CLI commands. Internal modules (checkpoint, hitl,
workflows, tools) are not part of a stable public API.
"""
