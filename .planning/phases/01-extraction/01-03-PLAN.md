---
phase: "01-extraction"
plan: "01-03"
type: "feature"
wave: 1
depends_on: ["01-02"]
files_modified:
  - "src/weekforge/agents/prompt_composer.py"
  - "src/weekforge/agents/summarize_agent.py"
  - "src/weekforge/models/workflow_state.py"
  - "src/weekforge/workflows/extraction.py"
  - "src/weekforge/cli.py"
  - "tests/agents/test_summarize_agent.py"
  - "tests/workflows/test_extraction.py"
autonomous: true
must_haves:
  truths:
    - "compose_static_instructions concatenates persona + guardrails and appending caveman if mode enabled."
    - "summarize_agent uses deps_type=SummarizeDeps and output_type=WeekSummary."
    - "ExtractionState JSON round-trips perfectly for checkpointing."
    - "HITL accept panel displays only the highlights and trend fields, not the raw JSON."
    - "run_summarize safely accumulates feedback into message_history."
  artifacts:
    - "src/weekforge/agents/summarize_agent.py"
    - "src/weekforge/workflows/extraction.py"
---

# Plan 01-03: Summary Agent & Workflow

## Goal
Add LLM synthesis on top of Tier-0 extraction. Define `summarize_agent` using Pydantic AI. Build the `extraction` workflow with a single HITL acceptance gate (showing only highlights as per our context decisions).

## Context
See `.planning/phases/01-extraction/01-CONTEXT.md` and `specs/steps/step-1c-summary-agent.md`.
- **Pydantic AI Output Format**: Loose Strings.
- **HITL Feedback UI**: Just highlights and trend.

<tasks>

<task>
<read_first>
- src/weekforge/agents/prompt_composer.py
- specs/steps/step-1c-summary-agent.md
</read_first>
<action>
Modify `src/weekforge/agents/prompt_composer.py`.
Add a new function: `def compose_static_instructions(caveman_mode: bool) -> str:`
Implementation:
- concatenate `load_coaching_persona()` and `load_coaching_guardrails()`.
- If `caveman_mode` is True, append `CAVEMAN_LITE_DIRECTIVE`.
- Join sections with `\n\n---\n\n`.
Return the composed string.
</action>
<acceptance_criteria>
- `grep "def compose_static_instructions" src/weekforge/agents/prompt_composer.py` succeeds.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/weekforge/models/user_profile.py
- src/weekforge/models/week_summary.py
- specs/steps/step-1c-summary-agent.md
</read_first>
<action>
Create `src/weekforge/agents/summarize_agent.py`.
1. Define a `SummarizeDeps` dataclass with `user_profile: UserProfile`, `implicit_feedback: ImplicitFeedback`, `plan_adherence: PlanAdherence | None`, and `tier0_summary_json: str`.
2. Define `_BASE_TASK_INSTRUCTIONS` literal exactly as listed in the step-1c spec.
3. Use `build_openai_model(resolve_llm_profile("reasoning"))`.
4. Initialize the Pydantic AI `Agent` named `summarize_agent` relying on `compose_static_instructions` + `_BASE_TASK_INSTRUCTIONS` for `instructions=`.
5. Add two `@summarize_agent.instructions` decorators: `_inject_user_profile(ctx)` and `_inject_tier0_facts(ctx)` matching the exact syntax from step-1c spec.
</action>
<acceptance_criteria>
- `grep "summarize_agent = Agent(" src/weekforge/agents/summarize_agent.py` succeeds.
- File imports `WeekSummary` and outputs it as `output_type`.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/weekforge/models/llm_call_cost.py
- src/weekforge/models/week_summary.py
</read_first>
<action>
Modify or create `src/weekforge/models/workflow_state.py`.
Define `ExtractionState(BaseModel)` containing fields exactly as specified in step-1c spec:
- `week_prefix: str`
- `overwrite_confirmed: bool = False`
- `tier0_summary: WeekSummary | None = None`
- `last_output: WeekSummary | None = None`
- `messages_json: list[dict[str, Any]] = Field(default_factory=list)`
- `calls: list[CallMetadata] = Field(default_factory=list)`
- `pending_feedback: str | None = None`
- `step: str = "overwrite_check"`
- `written_page_id: str | None = None`
- `started_at: datetime = Field(default_factory=lambda: datetime.now(UTC))`
</action>
<acceptance_criteria>
- `grep "class ExtractionState(BaseModel):" src/weekforge/models/workflow_state.py` succeeds.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/weekforge/checkpoint.py
- src/weekforge/hitl.py
- src/weekforge/workflows/e2e.py
- specs/steps/step-1c-summary-agent.md
- .planning/phases/01-extraction/01-CONTEXT.md
</read_first>
<action>
Create `src/weekforge/workflows/extraction.py`.
Define `def run_summarize(week_prefix: str, thread_id: str, store: CheckpointStore) -> None:`
1. Orchestrate state transitions via a while loop matching the steps in `ExtractionState`: `overwrite_check`, `load_context`, `tier0_extract`, `agent`, `accept`, `write`, `done`.
2. For now, leave `overwrite_check`, `load_context`, and `tier0_extract` as placeholders that set state fields if not doing actual Notion logic. But ensure the `agent` step runs `summarize_agent` if `step == "agent"`.
3. In `accept` step, implement `hitl_confirm` that renders ONLY `highlights` and `trend` per `01-CONTEXT.md`. E.g.: `context=Panel(f"Highlights:\n{highlights}\n\nTrend: {trend}")`.
4. If feedback provided, append to message history, reset state to `agent` to loop. Note maximum 3 iterations and warn.
5. In `write` step, raise `NotImplementedError("Implemented in step-1d")`.
</action>
<acceptance_criteria>
- `grep "def run_summarize" src/weekforge/workflows/extraction.py` succeeds.
- The `hitl_confirm` does not serialize the entire dict (as decided in 01-CONTEXT.md).
</acceptance_criteria>
</task>

<task>
<read_first>
- src/weekforge/cli.py
</read_first>
<action>
Modify `src/weekforge/cli.py`.
Locate `def summarize(week: int = typer.Argument(...))`.
Replace the NotImplementedError stub with:
`week_prefix = format_week_prefix(week)`
`thread_id = f"summarize-{week_prefix}"`
`store = CheckpointStore(...)`
`run_summarize(week_prefix, thread_id, store)`
Imports must be correctly wired.
</action>
<acceptance_criteria>
- `grep "run_summarize(" src/weekforge/cli.py` succeeds.
</acceptance_criteria>
</task>

<task>
<read_first>
- specs/steps/step-1c-summary-agent.md
</read_first>
<action>
Create `tests/agents/test_summarize_agent.py` and `tests/workflows/test_extraction.py`.
Implement unit tests covering basic round-trip and workflow looping. Fake the Notion and agent outputs.
</action>
<acceptance_criteria>
- Tests pass via `uv run pytest tests/agents/ tests/workflows/test_extraction.py`
</acceptance_criteria>
</task>

</tasks>
