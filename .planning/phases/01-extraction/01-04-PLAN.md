---
phase: "01-extraction"
plan: "01-04"
type: "feature"
wave: 1
depends_on: ["01-03"]
files_modified:
  - "src/weekforge/tools/week_summary_renderer.py"
  - "src/weekforge/tools/plan_state.py"
  - "src/weekforge/agents/plan_state_agent.py"
  - "src/weekforge/workflows/extraction.py"
  - "src/weekforge/models/workflow_state.py"
  - "tests/tools/test_week_summary_renderer.py"
  - "tests/tools/test_plan_state.py"
  - "tests/workflows/test_extraction_end_to_end.py"
autonomous: true
must_haves:
  truths:
    - "render_week_summary strictly complies with legacy string syntax (pipe delimiters, planned->actual)."
    - "Notion writes overwrite existing W## row if created, otherwise create natively."
    - "PLAN_STATE runs incrementally if exists, or bootstraps lazily inside Extraction loop."
    - "PlanState agent separates mechanical updates (Tier-0) from reasoning (Tier-2)."
    - "Write step does NOT emit entire contents inside the terminal — preserves Caveman UI."
  artifacts:
    - "src/weekforge/tools/week_summary_renderer.py"
    - "src/weekforge/tools/plan_state.py"
    - "src/weekforge/agents/plan_state_agent.py"
---

# Plan 01-04: Notion Write & PLAN_STATE

## Goal
Close the extraction loop. Render the LLM-derived `WeekSummary` into legacy string syntax and push back logic to Notion. Integrate `PLAN_STATE` processing (incremental merge or bootstrap mode).

## Context
See `.planning/phases/01-extraction/01-CONTEXT.md` and `specs/steps/step-1d-notion-write-and-plan-state.md`.
- **Bootstrapping PLAN_STATE**: Occurs inside the Extraction Loop.

<tasks>

<task>
<read_first>
- specs/steps/step-1d-notion-write-and-plan-state.md
- src/weekforge/models/week_summary.py
</read_first>
<action>
Create `src/weekforge/tools/week_summary_renderer.py`.
Implement `def render_week_summary(summary: WeekSummary) -> str:`.
Conform byte-for-byte to the `WEEK_SUMMARY` syntax rules in the legacy docs (pipe limits `name|status|done/total|pain|feedback`). 
Omit `PLAN_ADHERENCE` if None.
</action>
<acceptance_criteria>
- `grep "def render_week_summary" src/weekforge/tools/week_summary_renderer.py` succeeds.
</acceptance_criteria>
</task>

<task>
<read_first>
- specs/steps/step-1d-notion-write-and-plan-state.md
</read_first>
<action>
Create `src/weekforge/tools/plan_state.py`.
Define Pydantic `PlanState` schema reflecting the `training_week_summaries` singleton text structure.
Implement `def parse_plan_state(text: str) -> PlanState` and `def update_mechanical_fields(state: PlanState, week: WeekSummary) -> PlanState`.
Mechanical fields: appending weight chains, calculating averages, etc.
</action>
<acceptance_criteria>
- `grep "class PlanState(BaseModel):" src/weekforge/tools/plan_state.py` succeeds.
- Mechanical update logic isolates deterministic math from reasoning.
</acceptance_criteria>
</task>

<task>
<read_first>
- specs/steps/step-1d-notion-write-and-plan-state.md
- src/weekforge/agents/prompt_composer.py
</read_first>
<action>
Create `src/weekforge/agents/plan_state_agent.py`.
Define `PlanStateDeps(existing_plan_state: PlanState, new_week: WeekSummary)`.
Initialize `plan_state_agent = Agent(...)` for Tier-2 reasoning.
Extract subjective inputs (issues, trends, skip patterns) into `PlanState` updates.
</action>
<acceptance_criteria>
- `grep "plan_state_agent = Agent(" src/weekforge/agents/plan_state_agent.py` succeeds.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/weekforge/workflows/extraction.py
- src/weekforge/models/workflow_state.py
- specs/steps/step-1d-notion-write-and-plan-state.md
</read_first>
<action>
Update `src/weekforge/models/workflow_state.py` by adding `plan_state_check` and `plan_state_update` steps to the conceptual sequence inside `ExtractionState.step`.
Update `src/weekforge/workflows/extraction.py`:
1. In `write` block: Call `render_week_summary(state.last_output)`. Write to Notion (`notion.query` by `Week == week_prefix` first. If present -> `notion.update`, else -> `notion.create`). Then transition to `plan_state_check`.
2. Add `plan_state_check` block: Query Notion for `Week == "PLAN_STATE"`. If missing, construct empty/bootstrap skeleton. Transition to `plan_state_update`.
3. Add `plan_state_update` block: Construct `PlanStateDeps`, run `plan_state_agent` if required. Render state. Push back to Notion singleton `PLAN_STATE`. Set state to `done`.
</action>
<acceptance_criteria>
- Pydantic AI is used for `plan_state_agent`.
- Notion sync logic correctly guards against overwrites/creation vs update routes.
</acceptance_criteria>
</task>

<task>
<read_first>
- specs/steps/step-1d-notion-write-and-plan-state.md
</read_first>
<action>
Create corresponding test files: `tests/tools/test_week_summary_renderer.py`, `tests/tools/test_plan_state.py`, and `tests/workflows/test_extraction_end_to_end.py`. Complete unit test coverage for format conformance. Run tests.
</action>
<acceptance_criteria>
- Test suite executes clean. Test coverage ensures byte-for-byte syntax format reliability.
</acceptance_criteria>
</task>

</tasks>
